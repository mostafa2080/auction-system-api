# Node.js dependencies
node_modules/

# Optional: Logs and debugging files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: Environment variables file
.env

# Optional: Compiled output (for TypeScript, Babel, etc.)
/dist
/build

# Optional: Dependency lock files
package-lock.json
yarn.lock

# Optional: Editor/IDE specific files
.vscode/
.idea/
*.code-workspace

# Optional: Coverage report folder (if using a testing framework)
/coverage

# Optional: Output folders for build tools
/public
/build
/out

# Optional: Any other temporary or build-related files/directories
/tmp
*.log
